ripname: Run tests

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  lint-and-type-check:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout rippled
        uses: actions/checkout@sidechain
        with:
          repository: ripple/rippled
          ref: sidechain
          path: ./rippled

      - name: View files
        run: ls -al
